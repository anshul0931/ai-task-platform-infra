# NOTE: This is a TEMPLATE for local/demo use only.
# In real deployments, generate secrets via `kubectl create secret` or a
# secrets manager (Sealed Secrets / External Secrets / Vault) — never commit
# actual base64 secret values to source control.
apiVersion: v1
kind: Secret
metadata:
  name: ai-task-secrets
  namespace: ai-task-platform
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_STRONG_RANDOM_SECRET"
  INTERNAL_API_KEY: "REPLACE_WITH_STRONG_RANDOM_KEY"
  MONGO_INITDB_ROOT_USERNAME: "admin"
  MONGO_INITDB_ROOT_PASSWORD: "REPLACE_WITH_STRONG_PASSWORD"
